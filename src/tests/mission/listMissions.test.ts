describe("GET /api/mission (getMyMissions)", () => {

    test("Renvoie les missions assignées avec filtres et tri", async () => {
    });

    test("Retourne les missions triées par missionType", async () => {
    });

    test("Renvoie une liste vide si aucun résultat dans la plage de dates", async () => {
    });

    test("Renvoie toutes les missions sans filtre", async () => {
    });

    test("Renvoie 401 si l'utilisateur n'est pas authentifié", async () => {
    });
});